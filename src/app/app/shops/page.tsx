import { ShopsPage } from "@/views/public/shops-page";

export const metadata = {
  title: "Find a Barber — TrimLink",
  description: "Browse all premium barbershops in Ethiopia.",
};

export default function Page() {
  return <ShopsPage />;
}
