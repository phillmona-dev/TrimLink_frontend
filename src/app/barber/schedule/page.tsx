import { BarberSchedulePage } from "@/views/barber/barber-schedule-page";

export const metadata = {
  title: "My Schedule | TrimLink",
  description: "Manage your daily availability",
};

export default function Page() {
  return <BarberSchedulePage />;
}
