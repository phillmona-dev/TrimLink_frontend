import { HelpPage } from "@/views/public/help-page";

export const metadata = {
  title: "Help & Contact — TrimLink",
  description: "Frequently asked questions, contact information, and support for TrimLink.",
};

export default function Page() {
  return <HelpPage />;
}
