import { ShopsPage } from "@/views/public/shops-page";

export const metadata = {
  title: "Explore Shops — TrimLink",
  description: "Browse and search all active staffshops on TrimLink across Ethiopia.",
};

export default function Page() {
  return <ShopsPage />;
}
